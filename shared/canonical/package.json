{
  "name": "shared-canonical",
  "version": "0.0.0",
  "private": true,
  "description": "Canonical types and Zod schemas. The contract between integrations, business rules, and the frontend.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc",
    "lint": "eslint src"
  },
  "dependencies": {
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "typescript": "^5.5.0"
  }
}
